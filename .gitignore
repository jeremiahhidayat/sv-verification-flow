dv-lead-prep.md
session-notes.md
cocotb-guide.md
.venv/
# Reference TB copied in for style, not part of this project's verification
tb/sv/bitt_diff_tb.sv

# cocotb / Verilator build output (regenerated by `make`)
sim_build/
results.xml
__pycache__/
*.pyc
*.vcd
*.fst

# Questa / ModelSim output (regenerated by `make regress`)
# The compiled library. Anchored to tb/sv so it can't swallow an RTL or
# testbench directory that legitimately gets named "work" later.
tb/sv/work/
# Logs, per-run UCDBs, merged UCDB and the HTML coverage report -- everything
# tb/sv/Makefile writes under $(OUT).
tb/sv/out/
# Loose artifacts Questa drops in whatever directory vsim was launched from.
transcript
*.wlf
wlf*
modelsim.ini
*.ucdb
covhtml/
*_stacktrace.vstf
.vlog.args
.vsim.args
questa.ini